<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.PermissionResourceMapper">

	<resultMap type="com.xiliulou.electricity.entity.PermissionResource" id="PermissionResourceMap">
		<result property="id" column="id" jdbcType="INTEGER"/>
		<result property="name" column="name" jdbcType="VARCHAR"/>
		<result property="type" column="type" jdbcType="INTEGER"/>
		<result property="uri" column="uri" jdbcType="VARCHAR"/>
		<result property="method" column="method" jdbcType="VARCHAR"/>
		<result property="sort" column="sort" jdbcType="NUMERIC"/>
		<result property="parent" column="parent" jdbcType="INTEGER"/>
		<result property="desc" column="desc" jdbcType="VARCHAR"/>
		<result property="createTime" column="create_time" jdbcType="INTEGER"/>
		<result property="updateTime" column="update_time" jdbcType="INTEGER"/>
		<result property="delFlag" column="del_flag" jdbcType="INTEGER"/>
	</resultMap>

	<!--查询单个-->
	<select id="queryById" resultMap="PermissionResourceMap">
        select
          id, name, type, uri, method, sort, parent, `desc`, create_time, update_time, del_flag
        from electricity.t_permission_resource
        where id = #{id}
        and del_flag = 0
    </select>

	<!--查询指定行数据-->
	<select id="queryAllByLimit" resultMap="PermissionResourceMap">
        select
          id, name, type, uri, method, sort, parent, `desc`, create_time, update_time, del_flag
        from electricity.t_permission_resource
        limit #{offset}, #{limit}
    </select>

	<!--通过实体作为筛选条件查询-->
	<select id="queryAll" resultMap="PermissionResourceMap">
		select
		id, name, type, uri, method, sort, parent, `desc`, create_time, update_time, del_flag
		from electricity.t_permission_resource
		where del_flag = 0

	</select>

	<!--新增所有列-->
	<insert id="insertOne" keyProperty="id" useGeneratedKeys="true">
        insert into electricity.t_permission_resource(name, type, uri, method, sort, parent, `desc`, create_time, update_time, del_flag)
        values (#{name}, #{type}, #{uri}, #{method}, #{sort}, #{parent}, #{desc}, #{createTime}, #{updateTime}, #{delFlag})
    </insert>

	<!--通过主键修改数据-->
	<update id="update">
		update electricity.t_permission_resource
		<set>
			<if test="name != null and name != ''">
				name = #{name},
			</if>
			<if test="type != null">
				type = #{type},
			</if>
			<if test="uri != null and uri != ''">
				uri = #{uri},
			</if>
			<if test="method != null and method != ''">
				method = #{method},
			</if>
			<if test="sort != null">
				sort = #{sort},
			</if>
			<if test="parent != null">
				parent = #{parent},
			</if>
			<if test="desc != null and desc != ''">
				`desc` = #{desc},
			</if>
			<if test="createTime != null">
				create_time = #{createTime},
			</if>
			<if test="updateTime != null">
				update_time = #{updateTime},
			</if>
			<if test="delFlag != null">
				del_flag = #{delFlag},
			</if>
		</set>
		where id = #{id}
	</update>

	<!--通过主键删除-->
	<update id="deleteById">
        update  electricity.t_permission_resource set del_flag = 1 where id = #{id}
    </update>

	<select id="queryListByIds" resultMap="PermissionResourceMap">
		select
		id, name, type, uri, method, sort, parent, `desc`, create_time, update_time, del_flag
		from electricity.t_permission_resource
		where
		id in
		<foreach collection="list" item="p" separator="," open="(" close=")">
		 #{p}
		</foreach>

	</select>
</mapper>