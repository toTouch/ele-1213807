<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.PermissionResourceMapper">

	<!--通过实体作为筛选条件查询-->
	<select id="queryAll" resultType="com.xiliulou.electricity.entity.PermissionResource">
		select
		id, name, type, uri, method, sort, parent, `desc`, create_time, update_time, del_flag
		from t_permission_resource
		where del_flag = 0 order by sort

	</select>


	<select id="queryListByIds" resultType="com.xiliulou.electricity.entity.PermissionResource">
		select
		id, name, type, uri, method, sort, parent, `desc`, create_time, update_time, del_flag
		from t_permission_resource
		where
		id in
		<foreach collection="list" item="p" separator="," open="(" close=")">
		 #{p}
		</foreach>

	</select>
</mapper>
