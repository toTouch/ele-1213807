<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.StoreAmountMapper">


    <select id="accountList" resultType="com.xiliulou.electricity.entity.StoreAmount">
        select * from t_store_amount
        <where>
            1 = 1
            <if test="storeIds != null and storeIds.size()>0  ">
                and shop_id in
                <foreach collection="storeIds" item="id" index="index" open="(" close=")" separator=",">
                    #{id}
                </foreach>
            </if>
            <if test="startTime != null">
                and create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                and create_time &lt;= #{endTime}
            </if>
            <if test="tenantId != null">
                and tenant_id = #{tenantId}
            </if>
            and del_flag = 0
            order by create_time desc
            limit #{offset}, #{size}
        </where>
    </select>



    <!--通过主键删除-->
    <delete id="deleteByStoreId">
        update t_store_amount
        set del_flag    = 1,
            update_time = (unix_timestamp(now()) * 1000)
        where shop_id = #{id}
    </delete>


    <!--通过主键修改数据-->
    <update id="updateIdEmpontent">
        update t_store_amount
        <set>
            <if test="fresh.balance != null">
                balance = #{fresh.balance},
            </if>
            <if test="fresh.updateTime != null">
                update_time = #{fresh.updateTime},
            </if>
        </set>
        where id = #{old.id}
        <if test="old.balance != null">
            and balance= #{old.balance}
        </if>
    </update>



</mapper>
