<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.ElectricityCabinetPowerMapper">

    <resultMap type="com.xiliulou.electricity.entity.ElectricityCabinetPower" id="ElectricityCabinetPowerMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="eid" column="eid" jdbcType="INTEGER"/>
        <result property="sameDayPower" column="same_day_power" jdbcType="NUMERIC"/>
        <result property="sumPower" column="sum_power" jdbcType="NUMERIC"/>
        <result property="date" column="date" />
        <result property="createTime" column="create_time" jdbcType="INTEGER"/>
        <result property="updateTime" column="update_time" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ElectricityCabinetPowerMap">
        select
          id, eid, same_day_power, sum_power, `date`, create_time, update_time
        from electricity.t_electricity_cabinet_power
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ElectricityCabinetPowerMap">
        select
          id, eid, same_day_power, sum_power, `date`, create_time, update_time
        from electricity.t_electricity_cabinet_power
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="ElectricityCabinetPowerMap">
        select
          id, eid, same_day_power, sum_power, `date`, create_time, update_time
        from electricity.t_electricity_cabinet_power
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="eid != null">
                and eid = #{eid}
            </if>
            <if test="sameDayPower != null">
                and same_day_power = #{sameDayPower}
            </if>
            <if test="sumPower != null">
                and sum_power = #{sumPower}
            </if>
            <if test="date != null">
                and `date` = #{date}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insertOne" keyProperty="id" useGeneratedKeys="true">
        insert into electricity.t_electricity_cabinet_power(eid, same_day_power, sum_power, `date`, create_time, update_time)
        values (#{eid}, #{sameDayPower}, #{sumPower}, #{date}, #{createTime}, #{updateTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update electricity.t_electricity_cabinet_power
        <set>
            <if test="eid != null">
                eid = #{eid},
            </if>
            <if test="sameDayPower != null">
                same_day_power = #{sameDayPower},
            </if>
            <if test="sumPower != null">
                sum_power = #{sumPower},
            </if>
            <if test="date != null">
                `date` = #{date},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from electricity.t_electricity_cabinet_power where id = #{id}
    </delete>

</mapper>