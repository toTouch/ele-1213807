<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.EleRefundOrderMapper">

    <resultMap type="com.xiliulou.electricity.entity.EleRefundOrder" id="EleRefundOrderMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="refundOrderNo" column="refund_order_no" jdbcType="VARCHAR"/>
        <result property="orderId" column="order_id" jdbcType="VARCHAR"/>
        <result property="payAmount" column="pay_amount" jdbcType="NUMERIC"/>
        <result property="refundAmount" column="refund_amount" jdbcType="NUMERIC"/>
        <result property="status" column="status" jdbcType="OTHER"/>
        <result property="errMsg" column="err_msg" jdbcType="VARCHAR"/>
        <result property="delFlag" column="del_flag" jdbcType="OTHER"/>
        <result property="createTime" column="create_time" jdbcType="INTEGER"/>
        <result property="updateTime" column="update_time" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="EleRefundOrderMap">
        select
          id, refund_order_no, order_id, pay_amount, refund_amount, status, err_msg, del_flag, create_time, update_time
        from electricity.t_ele_refund_order
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryList" resultType="com.xiliulou.electricity.vo.EleRefundOrderVO">
        select
        tero.id as id, tero.refund_order_no as refundOrderNo, tero.order_id as orderId, tero.pay_amount as payAmount,
        tero.refund_amount as refundAmount, tero.status as status, tero.err_msg as errMsg, tero.del_flag as delFlag,
        tero.create_time as createTime, tero.update_time as updateTime ,tedo.uid as uid,tedo.name as name,tedo.phone as phone
        from electricity.t_ele_refund_order as tero
        left join electricity.t_ele_deposit_order as tedo  on  tero.order_id= tedo.order_id
        <where>
            <if test="query.refundOrderNo != null and query.refundOrderNo != ''">
                and tero.refund_order_no = #{refundOrderNo}
            </if>
            <if test="query.orderId != null and query.orderId != ''">
                and tero.order_id = #{query.orderId}
            </if>
            <if test="query.status != null">
                and tero.status = #{query.status}
            </if>
            <if test="query.beginTime != null">
                and tero.create_time &gt;=#{query.beginTime}
            </if>
            <if test="query.endTime != null">
                and tero.create_time &lt;=#{query.endTime}
            </if>
        </where>
        order by tero.update_time desc
        limit #{query.offset}, #{query.size}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="EleRefundOrderMap">
        select
          id, refund_order_no, order_id, pay_amount, refund_amount, status, err_msg, del_flag, create_time, update_time
        from electricity.t_ele_refund_order
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="refundOrderNo != null and refundOrderNo != ''">
                and refund_order_no = #{refundOrderNo}
            </if>
            <if test="orderId != null and orderId != ''">
                and order_id = #{orderId}
            </if>
            <if test="payAmount != null">
                and pay_amount = #{payAmount}
            </if>
            <if test="refundAmount != null">
                and refund_amount = #{refundAmount}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="errMsg != null and errMsg != ''">
                and err_msg = #{errMsg}
            </if>
            <if test="delFlag != null">
                and del_flag = #{delFlag}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insertOne" keyProperty="id" useGeneratedKeys="true">
        insert into electricity.t_ele_refund_order(refund_order_no, order_id, pay_amount, refund_amount, status, err_msg, del_flag, create_time, update_time)
        values (#{refundOrderNo}, #{orderId}, #{payAmount}, #{refundAmount}, #{status}, #{errMsg}, #{delFlag}, #{createTime}, #{updateTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update electricity.t_ele_refund_order
        <set>
            <if test="refundOrderNo != null and refundOrderNo != ''">
                refund_order_no = #{refundOrderNo},
            </if>
            <if test="orderId != null and orderId != ''">
                order_id = #{orderId},
            </if>
            <if test="payAmount != null">
                pay_amount = #{payAmount},
            </if>
            <if test="refundAmount != null">
                refund_amount = #{refundAmount},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="errMsg != null and errMsg != ''">
                err_msg = #{errMsg},
            </if>
            <if test="delFlag != null">
                del_flag = #{delFlag},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

</mapper>
