<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.StoreSplitAccountHistoryMapper">




	<select id="queryList" resultType="com.xiliulou.electricity.entity.StoreSplitAccountHistory">
        select store_id,create_time,split_amount,current_total_income,pay_amount,percent,order_id,type from t_store_split_account_history
        <where>
            <if test="orderId != null and ''!=orderId">
                and order_id = #{orderId}
            </if>
            <if test="storeId != null">
                and store_id = #{storeId}
            </if>
            <if test="startTime != null">
                and create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                and create_time &lt;= #{endTime}
            </if>
            <if test="tenantId != null">
                and tenant_id &lt;= #{tenantId}
            </if>
        </where>
        order by create_time desc
        limit #{offset},#{size}
    </select>

    <select id="queryCount" resultType="java.lang.Integer">
        select count(1) from t_store_split_account_history
        <where>
            <if test="orderId != null and ''!=orderId">
                and order_id = #{orderId}
            </if>
            <if test="storeId != null">
                and store_id = #{storeId}
            </if>
            <if test="startTime != null">
                and create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                and create_time &lt;= #{endTime}
            </if>
        </where>
    </select>
</mapper>
