<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.enterprise.EnterpriseCloudBeanOverviewMapper">
    <sql id="column">
        enterprise_id, allocation_cloud_bean, allocation_package_count, allocation_user_count,
        recycle_cloud_bean, recycle_package_count, recycle_user_count, can_recycle_cloud_bean,
        can_package_count, can_recycle_user_count, del_flag, tenant_id, create_time, update_time
    </sql>

    <select id="selectByEnterpriseId"
            resultType="com.xiliulou.electricity.entity.enterprise.EnterpriseCloudBeanOverview">
        select <include refid="column"/>
            from t_enterprise_cloud_bean_overview
            where enterprise_id = #{id} and del_flag = 0
    </select>
</mapper>

