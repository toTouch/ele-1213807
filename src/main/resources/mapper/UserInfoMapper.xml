<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.UserInfoMapper">


    <!--查询指定行数据-->
    <select id="queryList" resultType="com.xiliulou.electricity.entity.UserInfo">
        select
        tui.id as id, tui.uid as uid, tui.user_name as userName,tui.name as name,
        tui.phone as phone, tui.id_number as idNumber, tui.service_status as serviceStatus,
        tui.auth_status as authStatus,tui.usable_status as usableStatus, tui.del_flag as delFlag,
        tui.create_time as createTime,tui.update_time as updateTime,tui.tenant_id as tenantId
        from t_user_info as tui
        left join t_franchisee_user_info as tfui on tui.id=tfui.user_info_id
        where tui.del_flag=0
        <if test="query.name != null and query.name != ''">
            and tui.name = #{query.name}
        </if>
        <if test="query.phone != null and query.phone != ''">
            and tui.phone = #{query.phone}
        </if>
        <if test="query.tenantId != null">
            and tui.tenant_id = #{query.tenantId}
        </if>
        <if test="query.beginTime != null">
            and tui.create_time &gt;=#{query.beginTime}
        </if>
        <if test="query.endTime != null">
            and tui.create_time &lt;=#{query.endTime}
        </if>
        <if test="query.authStatus !=null">
            and tui.auth_status =#{query.authStatus}
        </if>
        <if test="query.serviceStatus == 0 or query.serviceStatus == 1">
            and tui.service_status =#{query.serviceStatus}
        </if>
        <if test="query.serviceStatus == 1">
            and tfui.service_status =#{query.serviceStatus}
        </if>
        <if test="query.serviceStatus == 2 or query.serviceStatus == 3">
            and tfui.service_status =#{query.serviceStatus}
        </if>
        order by tfui.create_time desc
        limit #{query.offset}, #{query.size}
    </select>



    <select id="queryListForBatteryService" resultType="com.xiliulou.electricity.entity.UserInfo">
        select
        tui.id as id, tui.uid as uid, tui.user_name as userName,tui.name as name,
        tui.phone as phone, tui.id_number as idNumber, tui.service_status as serviceStatus,
        tui.auth_status as authStatus,tui.usable_status as usableStatus, tui.del_flag as delFlag,
        tui.create_time as createTime,tui.update_time as updateTime,tui.tenant_id as tenantId
        from t_user_info as tui
        left join t_franchisee_user_info as tfui on tui.id=tfui.user_info_id
        where tui.del_flag=0
        <if test="query.name != null and query.name != ''">
            and tui.name = #{query.name}
        </if>
        <if test="query.phone != null and query.phone != ''">
            and tui.phone = #{query.phone}
        </if>
        <if test="query.tenantId != null">
            and tui.tenant_id = #{query.tenantId}
        </if>
        <if test="query.beginTime != null">
            and tui.create_time &gt;=#{query.beginTime}
        </if>
        <if test="query.endTime != null">
            and tui.create_time &lt;=#{query.endTime}
        </if>
        <if test="query.authStatus !=null">
            and tui.auth_status =#{query.authStatus}
        </if>
        <if test="query.memberCardExpireTime !=null">
            and tfui.member_card_expire_time=#{query.memberCardExpireTime}
        </if>
        <if test="query.serviceStatus == 0 or query.serviceStatus == 1">
            and tui.service_status =#{query.serviceStatus}
        </if>
        <if test="query.serviceStatus == 1">
            and tfui.service_status =#{query.serviceStatus}
        </if>
        <if test="query.serviceStatus == 2 or query.serviceStatus == 3">
            and tfui.service_status =#{query.serviceStatus}
        </if>
        order by tfui.member_card_expire_time desc
        limit #{query.offset}, #{query.size}
    </select>

    <select id="queryCount" resultType="java.lang.Integer">
        select COUNT(1)
        from t_user_info as tui
        left join t_franchisee_user_info as tfui on tui.id=tfui.user_info_id
        where tui.del_flag=0
        <if test="query.name != null and query.name != ''">
            and tui.name = #{query.name}
        </if>
        <if test="query.phone != null and query.phone != ''">
            and tui.phone = #{query.phone}
        </if>
        <if test="query.tenantId != null">
            and tui.tenant_id = #{query.tenantId}
        </if>
        <if test="query.beginTime != null">
            and tui.create_time &gt;=#{query.beginTime}
        </if>
        <if test="query.endTime != null">
            and tui.create_time &lt;=#{query.endTime}
        </if>
        <if test="query.authStatus !=null">
            and tui.auth_status =#{query.authStatus}
        </if>
        <if test="query.serviceStatus == 0 or query.serviceStatus == 1">
            and tui.service_status =#{query.serviceStatus}
        </if>
        <if test="query.serviceStatus == 1">
            and tfui.service_status =#{query.serviceStatus}
        </if>
        <if test="query.serviceStatus == 2 or query.serviceStatus == 3">
            and tfui.service_status =#{query.serviceStatus}
        </if>
    </select>


    <select id="homeThree" resultType="java.util.HashMap">
        select from_unixtime(create_time / 1000, '%Y-%m-%d') date, count(*) as count
        from t_user_info
        where del_flag=0
        and create_time &gt;= #{startTimeMilliDay} and create_time &lt;=#{endTimeMilliDay}
        <if test="tenantId != null ">
            and tenant_id = #{tenantId}
        </if>
        group by date
        order by date desc
    </select>



    <select id="homeOne" resultType="java.lang.Integer">
        select count(1)
        from t_user_info
        where del_flag=0
        and create_time &gt;=#{first}
        and create_time &lt;=#{now}
        <if test="tenantId != null">
            and tenant_id = #{tenantId}
        </if>
    </select>





</mapper>
