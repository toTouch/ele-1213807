<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.TemplateConfigMapper">
    
    <!-- 基础字段 -->
	<sql id="selectTemplateConfig">
       	select   id, battery_outtime_template, electric_quantity_remind_template, tenant_id, create_time, update_time, battery_member_card_expiring_template,car_member_card_expiring_template        from t_template_config
    </sql>
  

    <!--新增所有列-->
    <insert id="insertOne" keyProperty="id" useGeneratedKeys="true">
        insert into t_template_config(battery_outtime_template, electric_quantity_remind_template, tenant_id, create_time, update_time, battery_member_card_expiring_template,car_member_card_expiring_template)
        values (#{batteryOuttimeTemplate}, #{electricQuantityRemindTemplate}, #{tenantId}, #{createTime}, #{updateTime}, #{batteryMemberCardExpiringTemplate},#{carMemberCardExpiringTemplate})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update t_template_config
        <set>
            <if test="batteryOuttimeTemplate != null ">
                battery_outtime_template = #{batteryOuttimeTemplate},
            </if>
            <if test="electricQuantityRemindTemplate != null ">
                electric_quantity_remind_template = #{electricQuantityRemindTemplate},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="batteryMemberCardExpiringTemplate != null">
                battery_member_card_expiring_template = #{batteryMemberCardExpiringTemplate},
            </if>
            <if test="carMemberCardExpiringTemplate != null ">
                car_member_card_expiring_template = #{carMemberCardExpiringTemplate},
            </if>
        </set>
        where id = #{id} and tenant_id = #{tenantId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from t_template_config where id = #{id} and tenant_id = #{tenantId}
    </delete>

</mapper>
