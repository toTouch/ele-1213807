<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.JoinShareActivityRecordMapper">

	<!--通过实体作为筛选条件查询-->
	<select id="getJoinShareActivityExpired" resultType="com.xiliulou.electricity.entity.JoinShareActivityRecord">
        select
          id, uid, join_uid, start_time, expired_time, status,  create_time, update_time, tenant_id
        from t_join_share_activity_record
        where expired_time &lt;= #{expiredTime} and status =1
        limit #{offset}, #{size}
    </select>

</mapper>
