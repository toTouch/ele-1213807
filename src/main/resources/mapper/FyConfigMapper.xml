<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.FyConfigServiceMapper">
    <insert id="insert">
        insert into t_fy_config(tenant_id, store_code, merchant_code, channel_code, del_flag, create_time, update_time)
        values ( #{tenantId}, #{storeCode}, #{merchantCode}, #{channelCode}, #{delFlag}, #{createTime}, #{updateTime})
    </insert>
    <update id="updateByTenantId">
        update t_fy_config
        <set>
            <if test="storeCode != null and storeCode != ''">
                store_code = #{storeCode},
            </if>
            <if test="merchantCode != null and merchantCode != ''">
                merchant_code = #{merchantCode},
            </if>
            <if test="channelCode != null and channelCode != ''">
                channel_code = #{channelCode},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where tenant_id = #{tenantId}
    </update>
    
    
    <select id="selectByTenantId" resultType="com.xiliulou.electricity.entity.FyConfig">
        select id,
               tenant_id,
               store_code,
               merchant_code,
               channel_code,
               del_flag,
               create_time,
               update_time
        from t_fy_config
        where del_flag = 0
          and tenant_id = #{tenantId}
    </select>
</mapper>
