<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.lostuser.LostUserFirstOrderRecordMapper">
    <sql id="column">
          `id`,
          `uid` ,
          `merchant_id` ,
          `order_id` ,
          `order_source_id` ,
          `package_type` ,
          `del_flag` ,
          `tenant_id` ,
          `create_time` ,
          `update_time`
    </sql>
    
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into t_lost_user_first_order_record(uid, merchant_id, order_id, order_source_id, package_type, tenant_id, create_time, update_time)
        values (#{uid}, #{merchantId}, #{orderId}, #{orderSourceId}, #{packageType}, #{tenantId}, #{createTime}, #{updateTime})
    </insert>
    
</mapper>
