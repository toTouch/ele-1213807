<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.ServicePhoneMapper">
    <sql id="basic_field">
        id
        , phone_content, franchisee_id, tenant_id, del_flag, create_time, update_time
    </sql>
    
    <insert id="insertOne">
        insert into t_service_phone (phone_content, franchisee_id, tenant_id, del_flag, create_time, update_time)
        values (#{phoneContent}, #{franchiseeId}, #{tenantId}, #{delFlag}, #{createTime}, #{updateTime})
    </insert>
    
    <update id="update">
        update t_service_phone
        set phone_content = #{phoneContent},
            update_time   = #{updateTime}
        where id = #{id}
    </update>
    
    <delete id="deleteById">
        delete
        from t_service_phone
        where id = #{id}
    </delete>
    
    <select id="selectByTenantId" resultType="com.xiliulou.electricity.entity.ServicePhone">
        select
        <include refid="basic_field"/>
        from t_service_phone
        where tenant_id = #{tenantId}
        and del_flag = 0
        limit 1
    </select>

</mapper>
