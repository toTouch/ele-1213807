<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.userinfo.emergencyContact.EmergencyContactMapper">
    <!-- 基础字段 -->
    <sql id="basic_field">
        id
        , uid, emergency_name, emergency_phone, relation, tenant_id, del_flag, create_time, update_time
    </sql>
    
    <insert id="batchInsert">
        insert into t_emergency_contact(uid, emergency_name, emergency_phone, relation, tenant_id, create_time, update_time)
        values
        <foreach item="item" index="index" collection="list" separator=",">
            (#{item.uid}, #{item.emergencyName}, #{item.emergencyPhone}, #{item.relation}, #{item.tenantId}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>
    
    <update id="updateById">
        update t_emergency_contact
        <set>
            <if test="emergencyName != null">
                emergency_name = #{emergencyName},
            </if>
            <if test="emergencyPhone != null">
                emergency_phone = #{emergencyPhone},
            </if>
            <if test="relation != null">
                relation = #{relation},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id} and tenant_id = #{tenantId} and del_flag = 0
    </update>
    
    <delete id="deleteByUid">
        delete from t_emergency_contact where tenant_id = #{tenantId} and uid = #{uid}
    </delete>
    
    <select id="selectListByUid" resultType="com.xiliulou.electricity.entity.userinfo.EmergencyContact">
        select
        <include refid="basic_field"/>
        from t_emergency_contact
        where tenant_id = #{tenantId} and uid = #{uid} and del_flag = 0
    </select>

</mapper>




