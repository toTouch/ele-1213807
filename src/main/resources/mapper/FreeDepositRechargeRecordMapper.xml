<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.FreeDepositRechargeRecordMapper">

    <resultMap type="com.xiliulou.electricity.entity.FreeDepositRechargeRecord" id="FreeDepositRechargeRecordMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="freeRecognizeCapacity" column="free_recognize_capacity" jdbcType="INTEGER"/>
        <result property="operator" column="operator" jdbcType="INTEGER"/>
        <result property="tenantId" column="tenant_id" jdbcType="INTEGER"/>
        <result property="delFlag" column="del_flag" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="INTEGER"/>
        <result property="updateTime" column="update_time" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 基础字段 -->
    <sql id="selectFreeDepositRechargeRecord">
       	select   id, free_recognize_capacity, operator, tenant_id, del_flag, create_time, update_time        from t_free_deposit_recharge_record
    </sql>

    <!--查询单个-->
    <select id="selectById" resultMap="FreeDepositRechargeRecordMap">
        <include refid="selectFreeDepositRechargeRecord"/>
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="selectByPage" resultMap="FreeDepositRechargeRecordMap">
        <include refid="selectFreeDepositRechargeRecord"/>
        where del_flag=0 and tenant_id=#{tenantId}
        <if test="startTime != null">
            and create_time &gt;= #{startTime}
        </if>
        <if test="endTime != null">
            and create_time &lt;= #{endTime}
        </if>
        order by create_time desc
        limit #{offset}, #{size}
    </select>

    <select id="selectByPageCount" resultType="int">
        select
        count(1)
        from t_free_deposit_recharge_record
        where del_flag=0 and tenant_id=#{tenantId}
        <if test="startTime != null">
            and create_time &gt;= #{startTime}
        </if>
        <if test="endTime != null">
            and create_time &lt;= #{endTime}
        </if>
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="selectByQuery" resultMap="FreeDepositRechargeRecordMap">
        <include refid="selectFreeDepositRechargeRecord"/>
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="freeRecognizeCapacity != null">
                and free_recognize_capacity = #{freeRecognizeCapacity}
            </if>
            <if test="operator != null">
                and operator = #{operator}
            </if>
            <if test="tenantId != null">
                and tenant_id = #{tenantId}
            </if>
            <if test="delFlag != null">
                and del_flag = #{delFlag}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insertOne" keyProperty="id" useGeneratedKeys="true">
        insert into t_free_deposit_recharge_record(free_recognize_capacity, operator, tenant_id, del_flag, create_time, update_time)
        values (#{freeRecognizeCapacity}, #{operator}, #{tenantId}, #{delFlag}, #{createTime}, #{updateTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update t_free_deposit_recharge_record
        <set>
            <if test="freeRecognizeCapacity != null">
                free_recognize_capacity = #{freeRecognizeCapacity},
            </if>
            <if test="operator != null">
                operator = #{operator},
            </if>
            <if test="tenantId != null">
                tenant_id = #{tenantId},
            </if>
            <if test="delFlag != null">
                del_flag = #{delFlag},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from t_free_deposit_recharge_record where id = #{id}
    </delete>

</mapper>
