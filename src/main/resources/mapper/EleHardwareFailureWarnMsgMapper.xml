<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.EleHardwareFailureWarnMsgMapper">
    
    <select id="selectList" parameterType="com.xiliulou.electricity.queryModel.failureAlarm.EleHardwareFailureWarnMsgQueryModel" resultType="com.xiliulou.electricity.vo.failureAlarm.EleHardwareFailureWarnMsgVo">
        SELECT
            tenant_id as 'tenantId',
            cabinet_id as 'cabinetId',
            type,
            count( 1 ) as 'failureWarnNum'
        FROM
        t_ele_hardware_failure_warn_msg
        WHERE
            alarm_time <![CDATA[ >= ]]>  #{startTime}
            AND alarm_time <![CDATA[ <= ]]>  #{endTime}
        GROUP BY
        tenant_id,
        cabinet_id,
        type
    </select>
</mapper>
