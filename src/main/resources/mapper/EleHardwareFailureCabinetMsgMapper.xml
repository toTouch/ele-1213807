<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.EleHardwareFailureCabinetMsgMapper">
    
    <insert id="batchInsert">
        insert into t_ele_hardware_failure_cabinet_msg(id , tenant_id , cabinet_id , failure_count , warn_count , create_time) values
        <foreach item="item" index="index" collection="list" separator=",">
            (#{item.id}, #{item.tenantId}, #{item.cabinetId}, #{item.failureCount}, #{item.warnCount}, #{item.createTime})
        </foreach>
    </insert>
    <delete id="batchDelete">
        delete
        from t_ele_hardware_failure_cabinet_msg
        WHERE create_time <![CDATA[ >= ]]>  #{startTime}
          AND create_time <![CDATA[ <= ]]>  #{endTime}
    </delete>
</mapper>
