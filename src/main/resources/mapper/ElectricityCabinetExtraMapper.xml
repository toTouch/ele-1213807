<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.ElectricityCabinetExtraMapper">
    <!-- 基础字段 -->
    <sql id="basic_field">
        id, eid, sn, battery_count_type, tenant_id, del_flag, create_time, update_time
    </sql>
    
    <select id="selectByEid" resultType="com.xiliulou.electricity.entity.ElectricityCabinetExtra">
        select
        <include refid="basic_field"/>
        from t_electricity_cabinet_extra
        where eid = #{eid} and tenant_id = #{tenantId} and del_flag = 0
    </select>
    
    <insert id="insertOne" keyProperty="id" useGeneratedKeys="true">
        insert into t_electricity_cabinet_extra(eid, sn, battery_count_type, tenant_id, del_flag, create_time, update_time)
        values (#{eid}, #{sn}, #{batteryCountType}, #{tenantId}, #{delFlag}, #{createTime}, #{updateTime})
    </insert>
    
    <update id="update">
        update t_electricity_cabinet_extra
        <set>
            <if test="eid != null">
                eid = #{eid},
            </if>
            <if test="sn != null and sn != ''">
                sn = #{sn},
            </if>
            <if test="batteryCountType != null">
                battery_count_type = #{batteryCountType},
            </if>
            <if test="delFlag != null">
                del_flag = #{delFlag},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
    </update>
</mapper>
