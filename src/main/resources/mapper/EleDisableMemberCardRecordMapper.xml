<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.EleDisableMemberCardRecordMapper">


    <select id="queryList"  resultType="com.xiliulou.electricity.entity.EleDisableMemberCardRecord">
        select * from t_ele_disable_member_card_record
        <where>
            <if test="tenantId !=null">
                and tenant_id=#{tenantId}
            </if>
            <if test="uid !=null">
                and uid=#{uid}
            </if>
        </where>
        order by create_time desc
        limit #{offset},#{size}
    </select>


    <select id="queryCount" resultType="integer">
        select count(1) from t_ele_disable_member_card_record
        <where>
            <if test="tenantId!=null">
                and tenant_id=#{tenantId}
            </if>
        </where>
    </select>
</mapper>
