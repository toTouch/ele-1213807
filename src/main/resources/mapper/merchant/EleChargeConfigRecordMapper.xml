<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.merchant.EleChargeConfigRecordMapper">
    <!-- 基础字段 -->
    <sql id="basic_field">
        id, name, franchisee_id, store_id, eid, tenant_id, type, create_time, update_time, json_rule, operation_type, operation_time, operator, config_id
    </sql>
    
    <!--新增所有列-->
    <insert id="insertOne" keyProperty="id" useGeneratedKeys="true">
        insert into t_ele_charge_config_record
        <trim prefix="(" suffix=")" suffixOverrides=",">
            name, franchisee_id, store_id, eid, tenant_id, type, create_time, update_time, json_rule, operation_type, operation_time, operator, config_id
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            #{name}, #{franchiseeId}, #{storeId}, #{eid}, #{tenantId}, #{type}, #{createTime}, #{updateTime}, #{jsonRule}, #{operationType}, #{operationTime}, #{operator}, #{configId}
        </trim>
    </insert>

</mapper>




