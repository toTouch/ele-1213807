<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.merchant.EleChargeConfigRecordMapper">
    <!-- 基础字段 -->
    <sql id="basic_field">
        id, name, eid, type, json_rule, tenant_id, franchisee_id, store_id, create_time, update_time
    </sql>
    
    <!--新增所有列-->
    <insert id="insertOne" keyProperty="id" useGeneratedKeys="true">
        insert into t_ele_charge_config_record
        <trim prefix="(" suffix=")" suffixOverrides=",">
            name, eid, type, json_rule, tenant_id, franchisee_id, store_id, create_time, update_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            #{name}, #{eid},#{type},#{jsonRule},#{tenantId},#{franchiseeId},#{storeId},#{createTime},#{updateTime}
        </trim>
    </insert>

</mapper>




