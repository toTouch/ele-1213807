<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.merchant.MerchantPlaceBindMapper">
    
    <insert id="batchInsert" >
        insert into t_merchant_place_bind (merchant_id , place_id , bind_time ,un_bind_time ,type ,merchant_month_settlement ,tenant_id ,  del_flag,, create_time, update_time)
        values
        <foreach item="item" index="index" collection="list" separator=",">
            (#{item.merchantId}, #{item.placeId}, #{item.bindTime},  #{item.unBindTime}, #{item.type}, #{item.merchantMonthSettlement}, #{item.tenantId}, #{item.delFlag}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>
    <update id="batchUnBind">
        update t_merchant_place_bind
           set type = 0,
            un_bind_time = #{updateTime}
         <where>
             merchant_id = #{merchantId}
             and type = 1
             <if test="placeIdList != null and placeIdList.size() > 0">
                 and place_id in
                 <foreach collection="placeIdList" separator="," open="(" close=")" item="placeId">
                     #{placeId}
                 </foreach>
             </if>
         </where>
         
    </update>
</mapper>
