<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.merchant.MerchantWithdrawOldConfigInfoMapper">
    <sql id="column">
        id, tenant_id, franchisee_id, del_flag, create_time, update_time
    </sql>

    <select id="existsMerchantOldWithdrawConfigInfo"
            resultType="Integer">
        select 1 from t_merchant_withdraw_old_config_info where tenant_id = #{tenantId} and franchisee_id = #{franchiseeId} and del_flag = 0 limit 1
    </select>

</mapper>

