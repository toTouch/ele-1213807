<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.merchant.ChannelEmployeePromotionDayRecordMapper">
   
    <insert id="batchInsert">
        insert into t_channel_employee_promotion_day_record (channel_employees_id , tenant_id , day_first_money , day_renew_money , day_total_money ,
        fee_date ,del_flag ,create_time ,update_time)
        values
        <foreach item="item" index="index" collection="statisticList" separator=",">
            (#{item.channelEmployeesId}, #{item.tenantId}, #{item.dayFirstMoney}, #{item.dayRenewMoney},
            #{item.dayTotalMoney},#{item.feeDate},#{item.delFlag},#{item.createTime},#{item.updateTime})
        </foreach>
    </insert>
    <select id="queryList"
            resultType="com.xiliulou.electricity.entity.merchant.ChannelEmployeePromotionDayRecord">
        select
            channel_employees_id , tenant_id , day_first_money , day_renew_money , day_total_money ,day_balance_money,
        fee_date ,del_flag ,create_time ,update_time
        from t_channel_employee_promotion_day_record
        <where>
            del_flag = 0
            and fee_date <![CDATA[ >=  ]]>  #{startTime}
            and fee_date <![CDATA[ <=  ]]>  #{endTime}
        </where>
        limit #{offset},#{size}
    </select>
    
    <select id="selectListByFeeDate"
            resultType="com.xiliulou.electricity.entity.merchant.ChannelEmployeePromotionDayRecord">
        select
        channel_employees_id , tenant_id , day_first_money , day_renew_money , day_total_money ,day_balance_money,day_renew_balance_money,
        fee_date ,del_flag ,create_time ,update_time
        from t_channel_employee_promotion_day_record
        <where>
            del_flag = 0
            and fee_date <![CDATA[ >=  ]]>  #{startTime}
            and fee_date <![CDATA[ <=  ]]>  #{endTime}
            and tenant_id = #{tenantId}
            <if test="franchiseeIdList != null and franchiseeIdList.size() > 0">
                and franchisee_id in
                <foreach collection="franchiseeIdList" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
        </where>
        
    </select>

</mapper>
