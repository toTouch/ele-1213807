<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.ShareActivityRecordMapper">


	<!--通过实体作为筛选条件查询-->
	<select id="queryList" resultType="com.xiliulou.electricity.entity.ShareActivityRecord">
		select
		id, activity_id, code, uid, count,  create_time, update_time, tenant_id,status,available_count
		from t_share_activity_record
		<where>
			<if test="uid != null">
				and uid = #{uid}
			</if>
		</where>
		order by update_time desc
		limit #{offset}, #{size}
	</select>
	<select id="queryCount" resultType="java.lang.Integer">
		select count(1)
		from t_share_activity_record
		<where>
			<if test="uid != null">
				and uid = #{uid}
			</if>
		</where>
	</select>


</mapper>
