<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiliulou.electricity.mapper.TenantAppInfoMapper">

    <resultMap type="com.xiliulou.electricity.entity.TenantAppInfo" id="TenantAppInfoMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="type" column="type" jdbcType="VARCHAR"/>
        <result property="appid" column="appId" jdbcType="VARCHAR"/>
        <result property="appsecert" column="appSecert" jdbcType="VARCHAR"/>
        <result property="tenantId" column="tenant_id" jdbcType="INTEGER"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TenantAppInfoMap">
        select id,
               type,
               appId,
               appSecert,
               tenant_id,
               status,
               create_time
        from t_tenant_app_info
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TenantAppInfoMap">
        select id,
               type,
               appId,
               appSecert,
               tenant_id,
               status,
               create_time
        from t_tenant_app_info limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="TenantAppInfoMap">
        select
        id, type, appId, appSecert, tenant_id, status, create_time
        from t_tenant_app_info
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="type != null and type != ''">
                and type = #{type}
            </if>
            <if test="appid != null and appid != ''">
                and appId = #{appid}
            </if>
            <if test="appsecert != null and appsecert != ''">
                and appSecert = #{appsecert}
            </if>
            <if test="tenantId != null">
                and tenant_id = #{tenantId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insertOne" keyProperty="id" useGeneratedKeys="true">
        insert into t_tenant_app_info(type, appId, appSecert, tenant_id, status, create_time)
        values (#{type}, #{appid}, #{appsecert}, #{tenantId}, #{status}, #{createTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update t_tenant_app_info
        <set>
            <if test="type != null and type != ''">
                type = #{type},
            </if>
            <if test="appid != null and appid != ''">
                appId = #{appid},
            </if>
            <if test="appsecert != null and appsecert != ''">
                appSecert = #{appsecert},
            </if>
            <if test="tenantId != null">
                tenant_id = #{tenantId},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from t_tenant_app_info
        where id = #{id}
    </delete>

    <select id="queryByTenantIdAndType" resultMap="TenantAppInfoMap">
      select  id, type, appId, appSecert, tenant_id, status, create_time
        from t_tenant_app_info where type = #{appType} and tenant_id = #{tenantId}
    </select>

    <select id="queryByAppId" resultMap="TenantAppInfoMap">
       select id, type, appId, appSecert, tenant_id, status, create_time
        from t_tenant_app_info where type = #{appType} and appId = #{appId}
    </select>

</mapper>
